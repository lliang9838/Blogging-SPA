import { CanActivateViaAuthGuard } from './can-activate-via-auth-guard';

describe('CanActivateViaAuthGuard', () => {
  it('should create an instance', () => {
    expect(new CanActivateViaAuthGuard()).toBeTruthy();
  });
});
